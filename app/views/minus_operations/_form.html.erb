<%= form_for(minus_operation) do |f| %>
  <% if minus_operation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(minus_operation.errors.count, "error") %> prohibited this minus_operation from being saved:</h2>

      <ul>
      <% minus_operation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :sum %>
    <%= f.text_field :sum, :required => true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
